<p id="notice"><%= notice %></p>

<h1>Type Users</h1>

<table>
  <thead>
    <tr>
      <th>Tipo de Usuário</th>
      <th>Criar</th>
      <th>Atualizar</th>
      <th>Deletar</th>
      <th>Vizualizar</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @type_users.each do |type_user| %>
      <tr>
        <td><%= type_user.description_name %></td>
        <td><%= type_user.criar %></td>
        <td><%= type_user.atualizar %></td>
        <td><%= type_user.deletar %></td>
        <td><%= type_user.visualizar %></td>
        <td><%= link_to 'Show', type_user %></td>
        <td><%= link_to 'Edit', edit_type_user_path(type_user) %></td>
        <td><%= link_to 'Destroy', type_user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <% users = get_users(type_user.id) %>
      <% users.each do |user|%>
        <tr>
          <td><%=  user.first_name %></td>
        </tr> 
      <% end %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Type User', new_type_user_path %>
