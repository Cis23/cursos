<%= form_with(model: type_user) do |form| %>
  <% if type_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_user.errors.count, "error") %> prohibited this type_user from being saved:</h2>

      <ul>
        <% type_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description_name %>
    <%= form.text_field :description_name %>
  </div>

  <div class="field">
    <%= form.label :action %>
    <%= form.text_field :action %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
